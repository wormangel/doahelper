<%- model_class = Costume -%>
<div class="page-header">
  <h1>New costume - <%= @character.name %></h1>
</div>
<%= render :partial => 'form', :locals => {:url => character_costumes_path(@character)} %>
